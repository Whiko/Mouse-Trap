  \chapter*{Analyse}
   \setcounter{chapter}{1}
    \addcontentsline{toc}{part}{Analyse}
    \lhead{Analyse}
    
    \section{Architecture générale}
    
    \section{Fonctionnalités en détail}
    
    \section{Implémentation}
    
    \section{Tests et utilisation}
    
    \section{Conclusion et évolutions possibles}
    
    \section{Solution retenue pour la carte et son affichage}
      Nous avons décidé de représenter toutes les cartes dans des fichiers texte. Dans ces fichiers, chaque 1 représente un mur, chaque 2 représente
      une étoile, chaque 3 représente une pièce, et chaque 4 des cases qui n'affichent rien (c'est à cet endroit que les fantomes apparaissent).\\
      Dans le constructeur de la classe \texttt{Map}, on donne en paramètre un \texttt{String} contenant le chemin du fichier texte. Une fois le 
      fichier chargé dans un objet \texttt{FileInputStream}, je parcoure tous les caractères  un par un
    
    
    \section{Partie un joueur}
      \subsection*{Etats}
      
      \subsection*{Personnages}
	Pour chaque personnage nous avons créé une classe correspondante, c'est à dire une classe \texttt{JoueurPacman} et \texttt{Fantome}, 
	toutes les deux héritant de la classe abstraite \texttt{Joueur}. Nous allons d'abord nous intéresser à la classe JoueurPacman.\\
	Il y a plusieurs fonctions importantes dans cette classe :
	- la fonction \texttt{seDeplacer} et son équivalent pour le serveur \texttt{seDeplacerServeur}. Le principe est le même pour les deux 
	  fonctions.\\
	  Une suite de conditions teste les entrées clavier (dans la deuxième, la direction à prendre est donnée par un 
	  \texttt{String} en paramètre), lorsque la direction demandée est trouvée, le programme regarde s'il est possible de se déplacer dans cette
	  direction. Il vérifie d'abord si la case juste à côté du personnage dans la direction que l'on veut suivre est différente de 1 (1 est 
	  utilisé pour représenter les murs dans le fichier texte de la carte), sinon il vérifie que 